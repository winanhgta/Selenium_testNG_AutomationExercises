name: Automation Exercise CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

#      - name: Run Tests with Maven
#        # Chạy headless vì GitHub Runner không có màn hình giao diện
#        run: mvn clean test -Dheadless=true -Dmaven.test.failure.ignore=true
#
#      - name: Check target directory # Bước này cực kỳ quan trọng để debug
#        run: ls -R target/allure-results || echo "Directory not found"

#      - name: Get Allure history
#        uses: actions/checkout@v4
#        if: always()
#        continue-on-error: true
#        with:
#          ref: gh-pages
#          path: gh-pages
#
#      - name: Allure Report Action
#        uses: simple-elf/allure-report-action@master
#        if: always()
#        with:
#          allure_results: ${{ github.workspace }}/target/allure-results
#          allure_history: allure-history
#
#      - name: Deploy Allure Report to GitHub Pages
#        if: always()
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: allure-history
          # Bước này để đảm bảo thư mục history tồn tại, tránh lỗi Unknown khi chạy lần đầu

#      - name: Get Allure history
#        uses: actions/checkout@v4
#        if: always()
#        continue-on-error: true
#        with:
#            ref: gh-pages
#            path: gh-pages

#      - name: Allure Report Action
#        uses: simple-elf/allure-report-action@master
#        if: always()
#        with:
#              # Sử dụng đường dẫn tương đối chuẩn của Maven
#              allure_results: target/allure-results
#              allure_history: allure-history
#              # Thêm dòng này để copy history từ gh-pages vào report mới
#              keep_reports: 20
#
#      - name: Deploy Allure Report to GitHub Pages
#        if: always()
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: allure-history # Đẩy thư mục đã gom đủ dữ liệu lên

#      - name: Run Tests with Maven
#        run: mvn clean test -Dheadless=true -Dmaven.test.failure.ignore=true
#
#      - name: Allure Report Action
#        uses: simple-elf/allure-report-action@master
#        if: always()
#        with:
#                allure_results: target/allure-results
#                allure_history: allure-history
#                keep_reports: 20
#
#      - name: Deploy Allure Report
#        if: always()
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#                github_token: ${{ secrets.GITHUB_TOKEN }}
#                publish_dir: allure-history # Đảm bảo đúng folder này

      - name: Run Tests with Maven
      # Thêm -Dallure.results.directory để ép Maven ghi đúng chỗ
        run: |
            mkdir -p target/allure-results
            mvn clean test -Dheadless=true -Dmaven.test.failure.ignore=true -Dallure.results.directory=target/allure-results

      - name: Debug Allure Results
        if: always()
        run: |
          echo "Check if target exists:"
          ls -d target || echo "Target folder NOT found"
          echo "Check if allure-results exists:"
          ls -d target/allure-results || echo "allure-results folder NOT found"
          echo "List files in allure-results:"
          ls target/allure-results || echo "No files in allure-results"

      - name: List Allure Results (Debug)
        if: always()
        run: |
                echo "Kiểm tra thư mục target:"
                ls -d target/allure-results || echo "Không thấy thư mục!"
                ls target/allure-results || echo "Thư mục trống không có file json nào!"

      - name: Allure Report Action
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
                allure_results: target/allure-results
                allure_history: allure-history
                keep_reports: 20

      - name: Send Telegram Notification
        uses: appleboy/telegram-action@master
        if: always() # Luôn chạy kể cả test Pass hay Fail
        with:
          to: ${{ secrets.Telegram_To }}
          token: ${{ secrets.Telegram_Token }}
          format: html
          message: |
            <b>🚀 Kết quả chạy Test trên GitHub</b>
            <b>Dự án:</b> ${{ github.repository }}
            <b>Trạng thái:</b> ${{ job.status == 'success' && '✅ THÀNH CÔNG' || '❌ THẤT BẠI' }}
            <b>Link báo cáo:</b> https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}